---
name: knowledge-pipeline
description: 把视频、文章等信息源批量转成结构化 HTML 知识页并入库个人知识库。含五阶段流程、四档质量门禁、单一账本、断点续跑、防丢规则。触发词：媒体学习 / 批量转知识页 / 知识库入库 / 素材转写 / 知识沉淀管线
---

# knowledge-pipeline · 信息源 → 个人知识库

> 把看过就忘的信息源，变成可检索、可复习、可追问的结构化知识页。
> 本文件自包含：读完即可开工，不需要外部文档。

## 一、这是什么

一条异步沉淀管线。输入是信息源（视频、播客、文章），输出是**单文件 HTML 知识页**和**一份始终一致的账本**。

关键不在"把内容转成文字"，在**攒得住**：两百篇之后依然可检索、无噪声、没丢过东西。

## 二、五条核心原则

1. **未提交 = 不存在**。页面生成后当轮必须提交版本库。
2. **账本是唯一事实源**。素材状态、定档、产出路径只记在一处。
3. **质量门禁零例外**。没有经过定档的内容不得入库。
4. **只加不删**。任何批量操作默认可回滚，删除前先移位而不是直接删。
5. **人机同源**。同一批内容同时产出人看页面和 AI 可读索引。

## 三、路径占位符

使用前把下表替换成你的真实路径。**不要硬编码绝对路径到脚本里**，用环境变量或配置文件注入。

| 占位符 | 含义 | 建议值 |
|---|---|---|
| `$KB_ROOT` | 知识库根目录 | 自选，建议放在版本库内 |
| `$LEDGER` | 账本文件 | `$KB_ROOT/_meta/ledger.json` |
| `$INCOMING` | 过程文件区 | `$KB_ROOT/_incoming/` |
| `$SOURCE_LIST` | 待处理素材清单 | CSV 或 JSON，自选 |

目录约定：

```
$KB_ROOT/
├── _meta/           真源数据（账本、专家索引）
├── _incoming/       过程文件（media/ trans/），建议 gitignore
├── _archive/        每日快照，安全网
├── assets/          样式与脚本
├── index.html       根索引
└── <NN_域>/<主题>/  知识页，按域分组
```

## 四、五阶段流程

### 阶段 A · 取清单

**输入**：你的信息源（收藏夹、稍后读、订阅列表）
**动作**：拉取全量清单，与 `$SOURCE_LIST` 对比找出新增
**产出**：更新后的 `$SOURCE_LIST`，每条带唯一 `id`
**判据**：新清单条数 > 旧清单条数，且 id 无重复

唯一 id 的选择要谨慎——很多平台存在"短 id"和"完整 id"两套标识，用错会拉到别人的数据。

### 阶段 B · 获取内容

**输入**：`$SOURCE_LIST` 中状态为 `pending` 的条目
**动作**：下载媒体文件 / 抓取文章正文
**产出**：`$INCOMING/media/<id>.mp4` 或 `$INCOMING/trans/<id>.txt`
**判据**：文件存在且体积超过阈值（视频建议 >100KB，文本建议 >500B）

**本阶段不提供实现。** 不同平台有不同的获取方式和各自的服务条款，请自行实现且遵守对方的规则。

常见现实约束，提前做好预期管理：
- 批量连续请求容易触发平台的频率限制，需要设计退避与冷却队列
- 长任务过程中的会话/凭据会失效，需设计自动重建
- 单条失败不应中断整批，记入账本下次续跑

### 阶段 C · 转写

**输入**：媒体文件
**动作**：提取音频（16kHz 单声道即可）→ 语音转文字
**产出**：`$INCOMING/trans/<id>.trans.txt`
**判据**：文本非空且长度与音频时长成合理比例

实用建议：
- 小规模用 `faster-whisper` 的 `small` 档即可，性能够用
- **中文素材显式指定语言，非中文素材让模型自动检测**——强制指定错误语言会产出音译乱码
- 谨慎使用 VAD 过滤，参数不当会把整段音频判为静音，产出空文件
- 耗时约为音频时长的 0.3~0.5 倍（CPU 推理），批量几十条请按小时计，后台跑并留心跳日志

### 阶段 D · 质量门禁（零例外）

**这是整条管线最重要的一步，不可跳过、不可批量放行。**

逐篇通读转写稿，按下表定档，写入账本的 `quality` 字段：

| 档 | 判据 | 处置 |
|---|---|---|
| `high` | 体系化干货，信息密度高，标题与内容一致 | 完整入库 |
| `medium` | 有可提取的方法论，但夹杂冗余或跑题 | 只精炼有效部分 |
| `low` | 标题党、引流广告、内容空洞、泛娱乐 | 不入库 |
| `reject` | 与知识库主题无关 | 跳过 |

四条判据，逐条过：

1. **标题与内容是否一致** —— 最有效的单一指标，标题党一眼可辨
2. **是否有引流广告** —— 中后段突然出现公众号/加群/带货，通常意味着质量断崖
3. **信息密度如何** —— 通读后能否复述出 3 个以上具体知识点
4. **是否泛娱乐化** —— 观点输出多、事实和方法少

**收藏不等于优质。** 当初收藏只说明标题打动了你。宁缺毋滥，没有门禁的知识库在两百篇之后就是一堆噪声。

### 阶段 E · 写页入库

**输入**：定档为 `high` / `medium` 的转写稿
**动作**：按页面规范写成 HTML → 放进对应域目录 → 更新域索引 → 更新根索引 → 跑链接审计
**产出**：`$KB_ROOT/<NN_域>/<主题>/YYYY-MM-DD_主题.html`
**判据**：全库链接审计 0 坏链，才算完成

## 五、页面规范

模板见同项目 `templates/knowledge_page.html`。单文件、离线可读、无外部依赖。

一篇合格的知识页包含以下要素：

| # | 要素 | 说明 |
|---|---|---|
| 1 | 顶部返回条 | 相对路径返回上级索引 |
| 2 | 标题区 | 标题 + 标签 + 难度星级 + 元信息行（学习日期 / 素材来源 / 预计阅读） |
| 3 | 一句话总结 | 页面最上方，1-2 句说清核心结论 |
| 4 | 知识地图 | TOC 导航，5-9 节，锚点对齐 |
| 5 | 分节正文 | 每节一张卡片，带序号 |
| 6 | 对比表格 | 并列概念必用，比段落高效得多 |
| 7 | 提示 / 警告块 | 视觉上区分于正文 |
| 8 | 易混点与误区 | 单独一节，表格呈现"误区 vs 真相" |
| 9 | 自测题 | 折叠块，点击展开答案；**避免出元信息题**（考日期、考作者这类），要考理解和应用 |
| 10 | 页脚 | 素材来源与整理日期 |
| 11 | 笔记组件 | 可选，读者随手记疑问并导出，交给 agent 处理 |

两条硬性纪律：

- **不使用 emoji**。它们在不同平台渲染不一致，且会干扰文本处理。
- **相对路径铁律**。返回首页 `../../index.html`、域内 `../<主题>/`、跨域 `../../<域>/`。绝对路径会在知识库迁移或换设备时全盘失效。

大文件提示：写超长 HTML 时，生成工具可能截断。做法是分片写成临时文件，再用脚本拼接。

## 六、账本结构

完整定义见 `examples/ledger.schema.json`。核心字段：

| 字段 | 用途 |
|---|---|
| `id` | 素材唯一标识，整条管线的主键 |
| `status` | `pending` → `working` → `transcribed` → `done` / `failed` |
| `quality` | `high` / `medium` / `low` / `reject`，由 D 阶段写入 |
| `media` / `transcript` | 过程文件路径 |
| `page` | 成页路径 |
| `updated_at` | 最后更新时间 |

**同一批次只允许一个进程写账本。** 并发写 JSON 会互相覆盖，这是最容易踩且最难排查的坑。

断点续跑就靠它：重跑命令 = 跳过 `done` 的命令。

## 七、防丢原则

以下五条来自真实事故（一次误操作丢掉 90 页未提交内容），**零例外**。

1. **禁止对知识库执行 `git clean -fd`**，无论带不带路径参数。清理残留一律先 `git status --short` 人工核对，只做精确回退。
2. **禁止把 `git reset --hard` 当作回退手段**。需要回退先 `git stash push`（保留未跟踪文件）或先做全量快照。
3. **任何破坏性操作前**（reset / clean / rm / checkout -- .），先确认目标文件是否已跟踪；未跟踪文件必须先复制到备份区。
4. **页面建完即提交**。任何 HTML / MD / 资产一经生成或修改，当轮结束前必须提交。禁止跨轮持有未提交内容。
5. **依赖库同步提交**（CSS / JS / 字体等），禁止裸放在工作区。

附加建议：每日自动生成一次全量快照归档到 `_archive/`，作为最后的安全网。

## 八、反模式清单

这些都是踩过的坑，按阶段排列：

**工程层面**
- 后台脚本输出非 ASCII 字符，在非 UTF-8 默认编码环境下崩溃 → 启动即重配置 stdout 编码
- 把 Python 代码内联进 shell 命令执行，遇到引号嵌套必炸 → **一律写成 .py 文件再执行**
- 生成超大 HTML 时工具静默截断 → 分片写 + 脚本拼接
- 多个进程同时写同一个 JSON 账本 → 单写者原则

**内容层面**
- 强制给非中文音频指定中文转写 → 产出音译乱码
- 开启 VAD 过滤但参数不当 → 整段被判静音，产出空文件
- 自测题出成元信息题（考日期、考标题）→ 没有复习价值
- 用绝对路径链接页面 → 迁移即全断

**流程层面**
- 批量跑完再统一做质量门禁 → 一次几十篇的通读任务必然走过场。**转完一批就定档一批**
- 定档后不回写账本 → 下次重跑全部重来
- 跳过链接审计就宣布完成 → 坏链要等到用户点开才发现

## 九、检查清单

每个批次收尾时逐项确认：

- [ ] 账本中本批次条目状态全部为 `done` 或 `failed`，无 `working` 悬挂
- [ ] 所有 `high` / `medium` 条目都有对应的 `page` 路径
- [ ] `page` 路径指向的文件真实存在
- [ ] 全库链接审计 0 坏链
- [ ] 域索引与根索引已更新，计数与实际一致
- [ ] 本批次新增页面已提交版本库
- [ ] 无孤儿过程文件（有转写但既无页面也未标记跳过）

## 十、AI 索引（可选但推荐）

知识库攒到几十篇之后，让 agent 全文扫描既慢又容易丢上下文。做法是按主题聚合一份索引文件：

```
$KB_ROOT/_meta/experts/<主题>.md
```

每条记录包含：页面路径、核心摘要、关联标签。agent 先检索索引命中主题，再精读少数几篇。

索引由账本生成，不要靠正则扫 HTML——后者在目录结构调整后必然失真。
