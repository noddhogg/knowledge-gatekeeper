# Contributing

感谢你想让这个项目变好。先说清楚它是什么形态，免得白费力气。

## 这是什么

**一个 Agent Skill，不是软件。**

核心产物是 `SKILL.md`，把它放进 agent 的 skills 目录，agent 读完就知道该怎么干活。没有构建步骤，没有依赖，没有需要 `pip install` 的东西。

所以大部分贡献是**改文档**，不是写代码。改完不需要跑测试，但需要说清楚"为什么这么改更好"。

## 最有价值的三类贡献

### 一、门禁判定案例（最想要这个）

四档定档是整套方法的核心，也是最难的部分。真实案例比任何讨论都有用。

开 issue 时附上：

- 素材标题（不用给链接）
- 你定的档位
- 判据在你身上怎么走的（标题一致性 / 引流广告 / 信息密度 / 泛娱乐，哪条起了决定作用）
- 如果事后回看觉得判错了，为什么

特别欢迎这两类：

- **判错的** —— 定成 `high` 后发现是注水内容，或定成 `low` 后发现有真东西
- **难判的** —— 四条判据互相打架，比如信息密度高但有引流

### 二、反模式补充

`SKILL.md` 第八节的反模式清单来自真实踩坑。如果你在同类工程里踩过别的坑，欢迎补。

格式：一句话说现象，一句话说根因，必要时给一句可执行的规避方式。不要写抽象原则。

### 三、页面规范改进

`templates/knowledge_page.html` 的十一要素是实操总结。如果你发现某类知识用这套结构讲不清楚，说明缺了什么，欢迎提。

## 不接受什么

**平台专属的获取工具（下载器、抓站脚本）。**

`SKILL.md` 第四节的获取与转写两层**故意留空**。原因有两个：

1. 不同平台有不同的服务条款，本仓库不分发绕过它们的东西
2. 那部分已有更好的实现，本项目的定位是它们上面的治理层

相关 PR 会被关闭。想做的话，建议单独开仓库，本仓库可以在 README 里链接过去。

## 发布前的最低标准

本仓库的立场是：**宁可不够好，也不能有错。** 下限比上限重要。

一条内容要进 README 或 SKILL.md，必须先过这三问：

1. **这是真的吗？** 涉及外部工具、数据、行为的断言，发布前必须核实。核实方式是去看那东西本身（README、源码、实际运行结果），不是凭印象或二手描述。
2. **它可执行吗？** "注意质量"没有价值，"通读后能否复述出 3 个以上具体知识点"才有。凡是无法照做的表述，要么改具体，要么删掉。
3. **边界说清了吗？** 数据来自哪里、在什么条件下成立、哪些情况没验证过——都要写出来。不确定的部分标注"未验证"，比含糊带过强。

已经踩过一次：初版 README 声称 `take-notes` 的产出是转写稿，可以作为本项目的输入。实际它输出 HTML / Markdown / Anki，中间不暴露转写稿——那个衔接点根本不存在。已修正，并补了"已知局限"章节。

**发现自己写错了，直接改并说明，不要留着。** 一个诚实的更正比一个漂亮的错误有价值得多。

## 几条约束

改 `SKILL.md` 时容易踩的：

1. **不要用 emoji**。规则 37，各平台渲染不一致且干扰文本处理。
2. **不要写死绝对路径**。一律用 `$KB_ROOT` / `$LEDGER` 这类占位符，具体值从配置读。
3. **不要把手写维护当方案**。专家索引、统计数字这类必须由账本生成，凡是"记得手动更新"的设计都不合格。
4. **每条判据要可执行**。"注意质量"这种话没有价值，"通读后能否复述出 3 个以上具体知识点"才有。

## 流程

小改动直接提 PR。涉及定位、档位定义、页面规范的改动，**先开 issue 讨论**——这几处牵一发动全身，写完了才发现方向不对就浪费了。

Issue 和 PR 用中文或英文都可以。
