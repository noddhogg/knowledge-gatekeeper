# 更新记录

**这里有两套版本号，别混着看：**

| 编号 | 记在哪 | 当前值 | 什么时候动它 |
|---|---|---|---|
| **skill 版本** | 本文件的版本标题 + `SKILL.md` frontmatter 的 `metadata.version` | 见最新一条 | 方法论、门禁规则、账本 schema、文档任一变更 |
| **模板版本** | `templates/knowledge_page.html` 的 `<meta name="kb-template-version">` | **1.1.0** | **只有页面模板本身变了才动** |

两套编号**独立推进**。1.2.0 起的多次变更都是方法论与文档层面的，模板没动，
模板版本号因此一直停在 1.1.0 —— 这是正常的，不是漏改。

- 你想判断"要不要重新生成已有页面" → **只看模板版本**
- 你想判断"手上这份方法论是哪一版" → 看 skill 版本（`SKILL.md` 的 `metadata.version`）

改 skill 版本时要同步两处：**本文件的版本标题** 与 **`SKILL.md` 的 `metadata.version`**。

Git tag 形如 `v1.4.0`。**`v1.0.0` 没有 tag**：它是把初期若干 commit 合并记录成的，
无法对应到单个 commit，与其打一个指向错误快照的 tag，不如不打。

## 1.4.0 — 2026-08-30

**方法论修补 + 版本体系澄清**（模板未变，模板版本号仍为 1.1.0）

- **外部工具产出的笔记同样要过成页门禁。** 此前成页门禁只按"你自己生成的页面"
  这一路径描述，未定义外部产物的适用方式，而用法二（`take-notes` /
  `video2knowledge` 等端到端工具）恰恰是最可能被采用的路径——它绕过的正是
  本项目最不可替代的防注水机制。现在明确两道门禁**串联执行，不是二选一**：
  四档定档管"要不要进库"，成页三条管"以什么规格进库"。
  唯一区别是"打回"的含义：外部笔记**不能退回工具重跑**（重跑只会拿到同一套模板、
  同一批套话），只能自行改写或换素材。
- **账本 schema 强制「已入库 ⇒ 已定档」。** `status == "done"` 的条目必须带
  四档之一的 `quality` 与非空的 `quality_reason`；`status == "failed"` 必须带
  非空的 `fail_reason`。此前这些都只是散文约定，无人检查。结构版本 1.1 → 1.2。
- **新增 `quality_reason` 字段。** 账本此前**没有**定档理由的容身之处，
  只能塞进 `fail_reason`——而 `fail_reason` 的语义是技术失败。字段缺失导致示例
  把"标题与内容不一致"写成定档理由，与「标题是提示，不是判据」直接冲突（`5b09925`）。
- **`medium` 与成页门禁双向交叉引用。** `medium` 的处置是"只精炼有效部分"，
  与成页门禁的 ≥800 字要求正面冲突；逃生口（合并进主题页 / 降级要点页）写在层 ④，
  与定档（层 ③）不在同一层，此前没有任何指引。现在两侧都指向对方。

## 1.3.1 — 2026-08-28

**新增成页门禁**（模板结构未变，仅头部加注释，模板版本号仍为 1.1.0）

四档门禁只覆盖**输入端**（层 ③，这条素材值不值得进库）。成页在层 ④，
此前没有任何判据——而**素材过关 ≠ 页面过关**。本次补上这一环。

**方法**

- **三条硬指标**（成页后、入库前逐页过）：自有知识量 ≥ 800 字
  （去模板外壳与跨页重复后统计）／跨页重复率 ≤ 15%／页面总字数**不设下限**。
- **为什么字数不能当指标**：这与 1.2.0 的「标题是提示，不是判据」是**同一条原则**——
  不要用代理指标代替真指标。字数配额奖励的是「看起来一样长」，不是「有东西」；
  它不消除差异，只把差异抹平。
- **短源怎么办**：短视频不是「内容不够」，是**不配单独占一页**。
  3–5 条同源素材合成一个主题页，深度来自多视角交叉印证，而不是注水。
  合并后仍不达标的，说明素材本身就不该单独成页——**该合并，不该扩写**。
- **判定口径**（不附脚本，与本 skill 定位一致）：知识量须先剔除模板外壳再统计，
  否则「共用模板」会被误判成「跨页重复」；跨页重复率按标题切段、
  中文 8-gram Jaccard ≥50% 计。阈值标明为**经验值**，换语言换领域需重设。

**落地**

- **反模式清单**新增「成页层面」四条：字数当质量指标／AI 指令写进标题／
  同一段通用内容复制到多页／短视频单独成页再靠扩写撑长。
- **检查清单**新增**第二道门禁**四项勾选项，每批次必跑——这是最直接的防线。
- **模板**头部注释写死内容下限。此前模板只给结构骨架、不含任何内容下限，
  正是缺口的源头。
- **自检**：用新门禁回测项目自带 `examples/sample-page.html`——954 字、
  零标题泄漏、通过。说明方法论与示例本身没问题，缺的只是条款。

**参考案例**（仅说明这道门禁的必要性，非主要产出；案例会过时，方法不会）

某 C++ 学习知识库 41 页，素材定档全部合规，AI 批量成页时被要求
「每页补到 ≥2000 字」——页面总字数极差仅 533（1341–1874）而真实知识量
极差 1734（0–1734），重复样板占 80%，41 / 41 页标题残留 AI 指令共 237 处。
它通过了当时**全部七项检查**（账本状态全 `done`、`page` 路径存在、0 坏链、已提交）。
按新门槛回算：41 页真实知识合计仅 1.3 万字，只够 16 页；后 29 页合计仅 3530 字，
合并后只够 4 页。

## 1.3.0 — 2026-08-28

**定位与文档**（模板未变，模板版本号仍为 1.1.0）

- **定位调整**：获取层从「不做获取」改为「**集成获取，不做实现**」。
  理由：实测 `yt-dlp` 2026.06.09 版有 **1747 个提取器**，中美主流平台基本全覆盖。
  不做获取的实现（不写下载器），但做获取的集成（给清单与接入配置）。
- **主打「收藏精炼」**。一句话定位：**把你收藏了却没看的东西，变成你真的会看的东西**。
  它与质量门禁天生气合——**收藏 ≠ 优质，所以才需要筛**。
  相比「关键词全网搜索」（同质化严重，很多工具在做），收藏精炼的痛点更真实、
  更高频，且自带愧疚感。
- **新增**：`docs/platforms.md`——平台适配清单。逐条列平台 / 类型 / 提取器数 /
  接入方式 / 能力 / 限制，并在文末给出复核命令（数字随 yt-dlp 版本变化，
  是可自证的快照而非承诺）。
- **收紧「实测」的措辞**（诚实性问题，自查发现）：原文写"每条均为本机实测"，
  属过度承诺。实际分层是——
  - **提取器数量**：真跑 `yt-dlp --list-extractors` 计数，可复现
  - **平台有哪些入口**：从提取器名与源码 `_VALID_URL` 直读，非逐个点开验证
  - **能否真下载成功**：**没测过，一个平台都没真下过**
  - **「限制」栏的需登录/风控严/失败率高**：部分是推断，部分是经验**判断**
  已在文档顶部新增「先说清『实测』测到了哪一层」表格，并把判断项逐条用
  「（判断）」标出。本文的「支持」= yt-dlp 存在该提取器，**不等于**下载实测通过。
  - 已实测支持：B站19 · YouTube20 · Vimeo11 · SoundCloud9 · TikTok7 · 微博3 ·
    Twitter6 · 网易云音乐7 · QQ音乐6 · 优酷2 · 喜马拉雅2 · A站2 · 斗鱼2 · 虎牙2 ·
    腾讯视频2 · 抖音1 · 小红书1 · 知乎1 · 爱奇艺1 · 西瓜1 · 头条1 · 蜻蜓FM1 ·
    梨视频1 · Reddit1 · Apple Podcasts1
  - 实测**不支持**：快手（含 Kwai）、微信视频号、Spotify（DRM）、猫耳FM、
    好看视频、秒拍、播客 RSS（无通用提取器，但自解析成本很低）
  - **明确三条红线**：不写下载器 · 不绕风控 · 不碰付费 / 隐私 / DRM
- **新增**：`docs/platforms.md` 中标注 TikTok 的 `tiktok:effect` / `tiktok:sound` /
  `tiktok:tag` 三个提取器在 yt-dlp 中已被标注 `CURRENTLY BROKEN`；README 已知局限
  补充「该版本全库 128 个条目被标注 BROKEN」，避免读者把提取器数量当作能力保证。
- **明确**：付费类入口（B站 `BilibiliCheese` 课堂、Vimeo `vimeo:ondemand` 点播等）
  在清单中列出**是为了说明限制，不是发通行证**。
- **调整**：CONTRIBUTING「不接受什么」改写为「下载器与抓站脚本不接受，
  平台清单条目欢迎（须附实测输出）」，并把三条红线同步进该节。

## 1.2.0 — 2026-08-28

**方法论修正**（模板未变，模板版本号仍为 1.1.0）

- **四档判据重构**：「标题与内容是否一致」**从判据降级为核查提示**。
  原判据存在系统性误杀——有一类内容专门靠"不一致"来传播。典型例子：
  《你这样刷题，活该考低分》与《这道题的正解思路》，讲的可能是同一套
  有效解法，前者只是用了逆反心理做包装。用标题去推断内容，等于用包装
  判断实质；而在你已拿到完整转写稿时，这一步本就是多余的。
- **新的四条判据**（首要判据改为"可提取的知识量"）：
  1. 能提取出多少可复用的知识（首要）
  2. 讲的是事实方法还是纯观点
  3. 冗余占比（只影响 `high` 与 `medium` 之分，不直接判死）
  4. 情绪操纵是否替代了实质
- **明确**：使用逆反、恐惧、夸大等手法**本身不扣分**，只看它有没有替代掉知识。
- **明确**：判据分阶段——搜索 / 获取层可以用标题筛掉不相关的；
  收集 / 定档层必须**唯知识论**，只看内容不看包装。
- `low` 的定义从"标题党"改为"**拆开包装后**没有可复用的知识"。
- **新增**：「相关工具」章节。列出经过逐个核实的第三方工具——
  获取（`yt-dlp`）与转写（`faster-whisper`），以及四个端到端转化工具
  （`take-notes`、`video2knowledge`、`video-to-notebook`、`weixin-favor-kb`），
  均附链接。已明确标注这些是独立项目，本仓库不维护、不背书。
- **新增**：SKILL.md 指引 agent 从 README 的清单里取工具建议，不要凭印象编造。

## 1.1.0 — 2026-08-28

**模板**（`templates/knowledge_page.html`）

- **修复**：笔记 ID 未经转义拼接进 HTML 属性。由于 ID 来自 localStorage，
  外部脚本可写入恶意内容造成属性注入。现统一经 `safeId()` 限制为字母数字。
- **修复**：`persist()` 静默吞掉写入失败，且界面无条件显示"已保存"。
  现返回写入结果，失败时给出提示并回滚，避免界面与存储不一致。
  触发场景包括：存储容量耗尽、隐私模式、`file://` 下被限制。
- **修复**：`esc()` 补齐双引号与单引号转义，并注明属性值位置应另用 `safeId()`。
- **新增**：启动时检测 localStorage 可用性，不可用立即提示。
- **新增**：模板版本号 meta，便于追踪存量页面所用版本。

**文档**

- **修正**：快速开始曾要求复制并填写 `config.json`，但仓库内无任何代码读取它。
  已改为说明其用途，并标注仅交给 agent 使用时可完全跳过。
- **修正**：安装命令补上 `mkdir -p`，否则目标目录不存在时必然失败。
- **修正**：`take-notes` 等端到端工具的产出描述——它们输出 HTML / Markdown /
  Anki，不输出转写稿，原文承诺的衔接点并不存在。
- **新增**：已知局限章节（无可执行代码、数据来源单一、判据为启发式、
  未经外部验证、用法二判据为推导而非实测）。
- **新增**：快速开始的自检步骤。
- **新增**：`examples/sample-page.html`——按全部要素填充完成的样例产出页，
  内容为公开技术知识，便于直接判断这套规范的实际效果。
- **明确**："禁用 emoji"的边界。此前未定义，导致难度星级 ★☆（U+2605/2606
  单色符号）被误判。现写明：禁的是 U+1F000 及以上的彩色 emoji 与变体
  选择符，单色符号可用。

## 1.0.0 — 2026-08-28

首个公开版本。四档质量门禁、单一账本、防丢闭环、页面规范、AI 专家索引。
